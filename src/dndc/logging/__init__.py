"""Append-only JSONL event emitter and cost telemetry (D-008).

Note: this package shadows the stdlib `logging` name only for `from dndc import logging`;
absolute imports inside it resolve to the stdlib as normal.
"""
